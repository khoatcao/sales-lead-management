global:
  scrape_interval: 15s
  evaluation_interval: 15s

storage:
  tsdb:
    out_of_order_time_window: 10m

scrape_configs:
  - job_name: "sales-lead-api"
    static_configs:
      - targets: ["otel-collector:8889"]
